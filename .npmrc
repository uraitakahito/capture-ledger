# puppeteer に Chromium を取りに行かせない。docs の画面の絵を撮る (pnpm run docs:shots)
# ためだけの依存で、CI や普通の install には要らない (撮る人が初回だけ手で入れる)。
# pnpm-workspace.yaml の allowBuilds (puppeteer: false) と同じ意図の二重の保険。
PUPPETEER_SKIP_DOWNLOAD=true
